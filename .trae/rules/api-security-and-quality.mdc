---
description: API 设计、安全约束与错误处理规范
globs: "{app/api,lib}/**/*.{ts,tsx,js,jsx}"
alwaysApply: false
---

# API、安全与质量规范

## API 设计
- 接口响应结构保持一致：`success`、`data`、`error`。
- 错误文案需对用户友好，同时保证开发排查可追踪。

## 输入校验与权限
- 所有外部输入（body、query、params、webhook）必须验证与清洗。
- 写操作接口必须进行鉴权和权限校验，禁止默认信任前端。
- 生产环境错误响应不得暴露敏感细节（密钥、SQL、堆栈）。

## 安全与配置
- 敏感信息通过环境变量管理，禁止硬编码。
- 区分开发/测试/生产配置，避免跨环境污染。
- 涉及登录后台时应考虑会话安全、CSRF 与速率限制。

## 测试基线
- 关键接口至少覆盖成功路径与主要失败路径。
- 异常分支应有可验证行为，不允许“吞错”。
